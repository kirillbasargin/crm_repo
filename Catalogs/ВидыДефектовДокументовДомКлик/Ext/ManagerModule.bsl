
Процедура ОбработкаПолученияПредставления(Данные, Представление, СтандартнаяОбработка)
	
	Если ТипЗнч(Данные) = Тип("Структура") И Данные.Свойство("Value") Тогда
		СтандартнаяОбработка = Ложь;
		Представление = Данные.Value; //Лев(СтрЗаменить(Данные.НомерКарты, " ", ""), 4) + " " + Прав(СтрЗаменить(Данные.НомерКарты, " ", ""), СтрДлина(СтрЗаменить(Данные.НомерКарты, " ", "")) - 4);
	Иначе
		Возврат;
	КонецЕсли;
		
КонецПроцедуры

Процедура ОбработкаПолученияПолейПредставления(Поля, СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь;
	Поля.Добавить("Value");
	
КонецПроцедуры
