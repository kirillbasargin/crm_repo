
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если НЕ Параметры.Свойство("РезультатЗапроса") Тогда
		Отказ = Истина;
		Возврат;
	КонецЕсли;
	
	РезультатЗапроса = Параметры.РезультатЗапроса;
	
КонецПроцедуры
