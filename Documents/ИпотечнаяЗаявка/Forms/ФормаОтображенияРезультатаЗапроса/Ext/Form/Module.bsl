
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если НЕ Параметры.Свойство("РезультатЗапроса") Тогда
		Отказ = Истина;
		Возврат;
	КонецЕсли;
	
	РезультатЗапроса = ?(ТипЗнч(Параметры.РезультатЗапроса) = Тип("Структура"), УправлениеИпотечнымиЗаявкамиДомКлик.ПреобразоватьСтруктуруВJSON(Параметры.РезультатЗапроса), Параметры.РезультатЗапроса);
	
КонецПроцедуры
